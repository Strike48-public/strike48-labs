---
title: StrikeHub
description: The unified native desktop shell for Strike48 connector applications.
---

import MediaCard from "../../../components/MediaCard.astro";

StrikeHub is a Dioxus 0.6 desktop application that acts as a unified host shell for Strike48 connector apps. It discovers running connector processes, communicates with them over IPC, and renders each as an embeddable panel inside a single native window.

Connector apps are native desktop tools that integrate with the **Prospector Studio** agent platform. StrikeHub manages their lifecycle, authentication, and UI embedding so each connector can focus on its domain.

## What It Does

- **Connector sidebar** — Lists all known connectors with online/offline/checking status indicators
- **Embedded content area** — Renders the active connector's UI via an iframe backed by the `connector://` custom protocol
- **Dual transport** — Supports IPC (Unix domain sockets) and legacy TCP modes per connector
- **Authentication** — OIDC flow with Keycloak, automatic token injection into connector HTML
- **WebSocket bridging** — Single-port WsRelay bridges Dioxus liveview and Prospector Studio GraphQL subscriptions
- **Health monitoring** — Periodic health checks with automatic status updates
- **Configuration** — TOML-based connector config at `~/.config/strikehub/connectors.toml`

## Architecture

```
┌──────────────────────────────────────────────────────┐
│  StrikeHub (Dioxus Desktop)                          │
│                                                      │
│  ┌──────────┐  ┌──────────────────────────────────┐  │
│  │ Sidebar  │  │  Content Area                    │  │
│  │          │  │                                  │  │
│  │ ● Kube   │  │  ┌──────────────────────────┐   │  │
│  │ ● Pick   │  │  │ Embedded WebView         │   │  │
│  │          │  │  │ (liveview of active app)  │   │  │
│  │          │  │  │                          │   │  │
│  │          │  │  └──────────────────────────┘   │  │
│  │          │  │                                  │  │
│  └──────────┘  └──────────────────────────────────┘  │
│                                                      │
│  ┌──────────────────────────────────────────────────┐│
│  │ Status Bar: connectors online                    ││
│  └──────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────┘
```

Each connector runs as a **separate process** with its own Axum HTTP server. StrikeHub embeds the connector's UI via an iframe pointed at a `connector://` custom protocol URL. Orchestration (health checks, lifecycle management) happens over the same Unix domain socket.

## Connector Apps

The first two connector releases:

| App | Icon | Description |
|-----|------|-------------|
| KubeStudio | `server` | Kubernetes cluster management dashboard |
| Pick | `shield-alert` | Penetration testing toolkit |

## Crate Structure

```
strikehub/
├── Cargo.toml          (workspace root)
├── crates/
│   ├── sh-core/        (config, IPC runner, auth, proxy, WS relay)
│   └── sh-ui/          (Dioxus desktop UI components)
```

## Demos

<div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 1.5rem; margin: 1.5rem 0;">
  <MediaCard
    title="Launching Connectors"
    description="Starting KubeStudio and Pick connectors from the sidebar."
    iconName="rocket"
  />
  <MediaCard
    title="Switching Apps"
    description="Navigating between connector apps in the embedded content area."
    iconName="layout-dashboard"
  />
  <MediaCard
    title="Authentication Flow"
    description="OIDC sign-in with Keycloak and automatic token injection."
    iconName="lock"
  />
</div>

## Next Steps

- [Getting Started](/strikehub/getting-started/) — Install and run StrikeHub
- [Installation](/strikehub/guides/installation/) — Detailed build instructions
- [Configuration](/strikehub/guides/configuration/) — Environment variables and TOML config
- [Connectors](/strikehub/guides/connectors/) — How the connector system works
- [Debug Logging](/strikehub/help/debugging/) — Troubleshooting and debug logging
